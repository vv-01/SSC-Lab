#include <stdio.h>

int main() {
    int num1, num2, num3, sum;

    /*
     * all variables are declared locally
     * also program is written in c
     * these are multiline comments
     */

	
    printf("Enter three numbers: ");
    scanf("%d %d %d", &num1, &num2, &num3);

    //hello world

    sum = num1 + num2 + num3;

    //how r u


    printf("Sum: %d\n", sum);

    return 0;
}

